---
- hosts: all
  become: true
  vars_files:
    - vars/main.yml
  environment:
    DB_HOST: "{{ rds_state.instance.endpoint }}"
    DB_NAME: "{{ rds.db_name }}"
    DB_USER: "{{ rds.username }}"
    DB_PASS: "{{ rds.password }}"

  tasks:
    - import_tasks: tasks/getfroms3.yml
    - import_tasks: tasks/javainstall.yml
    - import_tasks: tasks/create_rds.yml
    - import_tasks: tasks/run_app.yml